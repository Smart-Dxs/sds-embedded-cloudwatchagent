version: 0.2

phases:
  install:
    runtime-versions:
      golang: 1.13
  pre_build:
    commands:
      - echo "Environment Variables:"
      - printenv
      - apt install rpm -y
      #- apt-get update
  build:
    commands:
      - cat go.mod
      - make test build
artifacts:
  files:
    - '**/*'
  discard-paths: no
